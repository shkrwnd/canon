[pytest]
# Pytest configuration
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Disable problematic plugins that cause dependency conflicts
# If deepeval is installed but causing issues, we can skip it
addopts = -p no:deepeval

